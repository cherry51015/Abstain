# --- Core backend ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1

# --- Orchestration ---
langchain==0.3.7
langgraph==0.2.39

# --- LLM provider (free-tier) ---
# Groq: OpenAI-compatible, generous free tier, fast inference. Recommended default.
langchain-groq==0.2.0
# Alternative free-tier option — swap in if you prefer Gemini's free quota instead:
# langchain-google-genai==2.0.4

# --- Retrieval (hybrid BM25 + FAISS, both fully open-source, no API needed) ---
rank-bm25==0.2.2
faiss-cpu==1.9.0
sentence-transformers==3.2.1 # open-source embedding model, runs locally, no API cost

# --- Storage ---
sqlalchemy==2.0.35
psycopg2-binary==2.9.9   # PostgreSQL driver — swap for sqlite3 (stdlib) if you skip Postgres setup

# --- Eval harness ---
pandas==2.2.3
numpy==1.26.4

# --- Testing ---
pytest==8.3.3

# --- Containerization ---
# (Docker itself isn't a pip package — see Dockerfile in repo root)
